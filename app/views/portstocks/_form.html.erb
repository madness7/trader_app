<%= form_for(@portstock) do |f| %>
  <% if @portstock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@portstock.errors.count, "error") %> prohibited this portstock from being saved:</h2>

      <ul>
      <% @portstock.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :stock_id %><br />
    <%= f.collection_select :stock_id, Stock.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :portfolio_id %><br />
    <%= f.collection_select :portfolio_id, @portfolios, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :price_paid %><br />
    <%= f.text_field :price_paid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
